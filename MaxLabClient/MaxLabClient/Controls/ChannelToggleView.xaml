<UserControl
    x:Class="TimeToShineClient.Controls.ChannelToggleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeToShineClient.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Margin="0,18,0,-18">
    
    <Grid >
        <ToggleButton IsChecked="{Binding IsToggled, Mode=TwoWay}" Width="100" Height="120" Content="{Binding ChannelNumber}" Foreground="White" Background="#66000000" FontSize="26"></ToggleButton>
    </Grid>
</UserControl>
